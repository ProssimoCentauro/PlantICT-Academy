public class MyClass {
    static boolean contains(String str, String sub) {
        return (str.indexOf(sub) == -1) ? false : true;
    }

    static boolean are_equals(String str1, String str2) {
        return (str1.equals(str2));
    }
}
